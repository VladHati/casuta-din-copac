# Site static — Netlify publica direct continutul repo-ului (HTML + PDF), fara build.
# PDF-urile sunt commit-uite in repo (generate local cu build_pdfs.py).
[build]
  publish = "."

[[headers]]
  for = "/PDF/*"
  [headers.values]
    Content-Type = "application/pdf"
